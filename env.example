# Copie para ".env" na raiz do projeto (não commite o .env).
#
# --- Opção A: Postgres local com Docker ---
#   docker compose up -d
#   npx prisma migrate deploy
#   npm run dev
#
DATABASE_URL="postgresql://isotank:isotank_local@localhost:5432/isotank?schema=public"

# Remoto (Render, Neon, etc.): o servidor ativa SSL no driver `pg` se não for localhost.
# Só precisa definir se quiseres forçar: DATABASE_SSL=true | false
# DATABASE_SSL=true

JWT_SECRET=ceinspec_isotank_dev_local
NODE_ENV=development
STORAGE_MODE=database

# Opcional: e-mail do engenheiro para assinatura fallback (geração do laudo)
# ENGENHEIRO_EMAIL=diego.fonseca@grupocesari.com.br
